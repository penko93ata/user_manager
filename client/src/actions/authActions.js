import {
  REGISTER_SUCCESS,
  REGISTER_FAIL,
  LOGIN_SUCCESS,
  LOGIN_FAIL,
  USER_LOADED,
  AUTH_ERROR,
  LOGOUT,
  CLEAR_ERRORS
} from './types';

// Register user
export const register = formData => async dispatch => {
  // TO-DO
};

// Login user
export const login = formData => async dispatch => {
  // TO-DO
};
